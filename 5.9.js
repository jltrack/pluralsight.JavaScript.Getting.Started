// Boolean Variables
// True or False
let saved = true;
saved = !saved;
showMessage(saved);

/* trying notes in Bear instead of PluralSight notes
let saved = false;
showMessage(typeof saved);
// prints boolean

let saved = false;
showMessage(saved);
// prints false

let saved = true;
showMessage(saved);
//prints true

let saved = true;
saved = !saved;
showMessage(saved);

//flips it, prints false because it is true and it prints false because it is ! which is not
*/
