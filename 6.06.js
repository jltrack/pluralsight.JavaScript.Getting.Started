/*

(condition) ? true-statement : false-statement;

e.g.

let message = (price > 10) ? 'expensive' : 'cheap';

showMessage(message); //expensive 

*/

let price = 20;

(price > 10) ? showMessage('yes') : showMessage('no');

// yes shows up. If you switch > to < then it changes to no
